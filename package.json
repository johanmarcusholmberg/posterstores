{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "concurrently --kill-others-on-fail --names api,web,canvas \"PORT=8080 pnpm --filter @workspace/api-server run dev\" \"PORT=26052 BASE_PATH=/ pnpm --filter @workspace/poster-store run dev\" \"PORT=8081 BASE_PATH=/__mockup pnpm --filter @workspace/mockup-sandbox run dev\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^10.0.3",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
